[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python manage.py migrate --noinput && python manage.py collectstatic --noinput && uvicorn backend_config.asgi:application --host 0.0.0.0 --port $PORT"
